name: Git Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  git_checkout:
    name: Test Git Checkout
    runs-on: ubuntu-latest
    container: aswf/ci-opencue:2020.5
    steps:
      # /opt/rh/rh-git218/root/usr/bin
      - run: |
          echo "echo foo" > script.txt
          source script.txt
      - run: scl_source enable rh-git218
      - run: echo $PATH
      - uses: actions/checkout@v2
      - name: Test Git
        run: git status
